# v2.7.3 (Hotfix)

## Bug Fixes
- Fixed nil table error when tracking sliders
- Clear uiSliders table when opening settings panel to avoid tracking old sliders
- Replaced CallOld() with direct original function reference to avoid nil error
